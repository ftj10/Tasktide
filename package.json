{
  "name": "weekly-todo-workspace",
  "private": true,
  "version": "1.6.0",
  "scripts": {
    "test": "npm --prefix backend run test && npm --prefix frontend run test:run",
    "test:smoke": "npm --prefix backend run test:smoke && npm --prefix frontend run test:smoke",
    "test:behavior": "npm --prefix backend run test:behavior && npm --prefix frontend run test:behavior",
    "test:backend": "npm --prefix backend run test",
    "test:frontend": "npm --prefix frontend run test:run"
  }
}
